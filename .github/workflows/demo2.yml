
name: my demo

on:
  push:
    branches: ["master"]


jobs:
  demojob:
    runs-on: ubuntu-latest
    steps:
    - name: 'hello'
      run: |
       git clone https://github.com/Ruchitha1230/web_node.git
       ls
       cd web_node
       ls
