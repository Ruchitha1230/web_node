
name: my demo

on:
  # push:
  #   branches: ["master"]
  pull_request: 
    types: 
      - opened
      - edited
      - reopened
      - synchronize
jobs:
  demojob:
    runs-on: ubuntu-latest
    steps:
      - name: display
        run: echo "hello world"
       
